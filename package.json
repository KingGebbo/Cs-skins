{
  "name": "cs2-pump-detection",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@vercel/postgres": "^0.8.0",
    "axios": "^1.6.8"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "dev": "vercel dev",
    "build:frontend": "cd frontend && npm install && npm run build"
  }
}
